window.WWR_CONFIG = {
  // Leave empty to load ./data/unified-rankings.json from the same FTP site.
  // Later you can set this to a GitHub Pages or raw GitHub URL for automatic JSON hosting.
  dataUrl: ""
};
